- name: Execute ansible-pull
  hosts: localhost
  connection: local
  become: true
  vars_files:
    - vars/vars.yml
    - vars/secrets.yml

  pre_tasks:
    - name: Update apt repositories
      ansible.builtin.apt:
        update_cache: true
      changed_when: false
      when: ansible_os_family == "Debian"
    - name: Update dnf repositories
      ansible.builtin.dnf:
        update_cache: true
      changed_when: false
      when: ansible_os_family == "RedHat" or ansible_facts['distribution'] == 'MOS'
    - name: Ensure presence of /var/cache/ansible
      ansible.builtin.file:
        path: /var/cache/ansible
        state: directory
        owner: root
        group: root
        mode: '0644'

  tasks:
    - name: Change repositories to Yandex mirrors
      ansible.builtin.include_tasks: tasks/repositories.yml

    # Install packages
    - name: Install packages
      ansible.builtin.include_tasks: tasks/packages.yml

    - name: Install RustDesk
      ansible.builtin.include_tasks: tasks/rustdesk.yml
      when: ansible_local.workstation.role.name | default('pupil') == 'staff' # Only for staff workstations

    # Edit packages
    - name: Edit packages
      ansible.builtin.include_tasks: tasks/thonny.yml # Thonny needs to be...edited a little bit. Removes the flag.
      when: ansible_local.workstation.role.name | default('pupil') == 'pupil' # Only relevant for pupil workstations.

    - name: Add wallpapers & icons
      ansible.builtin.include_tasks: tasks/wallpapers.yml

    # Make users, including ansible user for automatic pulling
    - name: Make users
      ansible.builtin.include_tasks: tasks/users.yml

    # Set cron jobs
    - name: Set cron jobs
      ansible.builtin.include_tasks: tasks/cron.yml

    # Turn on manual login, so users can type in their domain usernames.
    - name: Configure the login screen
      ansible.builtin.include_tasks: tasks/login_screen.yml

    # Install and start autofs
    - name: Install and start autofs
      ansible.builtin.include_tasks: tasks/autofs.yml

    # Set Firefox policies using a policies.json file
    - name: Set Firefox policy
      ansible.builtin.include_tasks: tasks/firefox.yml

     # Install Veyon and import its configuration. Prevent pupils from launching Veyon Master.
    - name: Install Veyon
      ansible.builtin.include_tasks: tasks/veyon.yml

    # Install and set up NetBird
    # - name: Install and set up NetBird
    #   ansible.builtin.include_tasks: tasks/netbird.yml